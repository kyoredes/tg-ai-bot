# Корневой файл — только для справки.
# Каждый сервис использует свой .env в своей папке.
# Docker Compose использует deploy/.env

# deploy/.env
BOT_TOKEN=
COMMON_PUB_KEY=secret
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-prod!!

# gateway-service/.env  → см. gateway-service/.env.example
# auth-service/.env       → см. auth-service/.env.example
# subscription-service/.env → см. subscription-service/.env.example
# aiogram-service/.env    → см. aiogram-service/.env.example
